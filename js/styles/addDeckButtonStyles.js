import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        height: '100%',
        width: 60,
        justifyContent: 'center',
        alignItems: 'center',
    },
    buttonText: {
        fontSize: 30,
    },
});