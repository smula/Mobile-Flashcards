import React from 'react';
import { View, Text } from 'react-native';

const DeckView = ({ navigation }) => (
    <View>  
        <Text>
            hello this is the deck view
        </Text>
    </View>
)

export default DeckView;